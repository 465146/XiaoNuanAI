[build]
builder = "nixpacks"

[build.build_args]
# Python + Node.js 双环境（Node.js 用于 OpenClaw Gateway）
NIXPACKS_PKGS = "python313, nodejs22"

[deploy]
startCommand = "python main.py"
healthcheckPath = "/api/health"
restartPolicyMaxRetries = 3

[service]
port = 8000
