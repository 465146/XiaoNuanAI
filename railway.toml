[build]
builder = "nixpacks"

[build.build_args]
NIXPACKS_PKGS = "python313"

[deploy]
startCommand = "python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/api/health"

[service]
port = 8000
